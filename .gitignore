# Internal drafts and secrets (never commit)
.drafts/
.secrets/
.env
.env.*
!.env.example

# Agent acceptance ephemeral DB (reports stay under results/)
tests/agent-acceptance/sandbox/db/*.sqlite3

# OS / editor noise
.DS_Store
*.log
