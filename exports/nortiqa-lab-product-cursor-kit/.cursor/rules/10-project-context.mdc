---
description: Verified repository context for nortiqa-lab/.github org profile and agent kit
alwaysApply: true
---

# Project context (evidence-only)

## Identity

- **GitHub remote:** `nortiqa-lab/.github` (org profile repository)
- **Entity:** Nortiqa Lab only
- **Purpose:** Host the autonomous agent team kit (`agents/`) and public org profile (`profile/README.md`)
- **Maturity:** Operational kit v1 (versionable). Notion canon remains separate.
- **Motto:** Primero funcional. Después excelente. Siempre: lo mejor o nada.

## What this repo is NOT

- Not the primary product/ops application codebase.
- Working product/ops code: `https://github.com/giovanyalbea-dotcom/nortiqa-lab`
- Production services: VPS referenced as SC2027 / `nortiqalab.com` (see bootstrap; do not assume host paths from this repo alone)

## Confirmed layout

| Path | Role |
|------|------|
| `AGENTS.md` | Root shared AI context |
| `CLAUDE.md` | Claude-shaped agent pointer |
| `agents/` | Team kit: rules, autonomy, roles, prompts, runbooks |
| `docs/shared-ai-memory/` | Handoff template + session logs |
| `docs/dev/` | DEV Cursor/dev workflow docs (draft) |
| `profile/README.md` | Public org profile content |
| `.cursor/rules/` | Cursor persistent rules |

## Stack detected in this repository

- Markdown documentation and agent prompts only.
- **No** `package.json`, Dockerfiles, Compose files, GitHub Actions workflows, language lockfiles, or app test runners found in-repo (as of audit commit baseline).

## Environments (from local kit / bootstrap — treat as map, not invent)

| Surface | Note |
|---------|------|
| Local / this git repo | Versionable kit + profile |
| Public site (SC2027) | `https://nortiqalab.com` |
| API health (SC2027) | `https://api.nortiqalab.com/health` |
| n8n (SC2027) | `https://n8n.nortiqalab.com/` |
| MCP (SC2027) | `https://mcp.nortiqalab.com/` (401 expected) |
| ERP Odoo/Nerva (`ERP-Nortiqa-Lab`) | `https://erp.nortiqalab.com/` |
| Metabase (`ERP-Nortiqa-Lab`) | `https://bi.nortiqalab.com/` |
| n8n on ERP host | `https://flow.nortiqalab.com/` |
| Canon | Notion `MEM-NL-ROOT-001` |

Nortiqa-owned ERP stack ≠ entity “ERP Gio+Edson”. Use ERP runbooks under `agents/runbooks/erp-nortiqa-lab-*.md`.

## Commands confirmed for this repo

- Git inspection: `git status`, `git log`, feature-branch PR flow per `agents/AUTONOMY.md`
- Public health GETs: `agents/runbooks/ops-public-health.md`
- App install / build / unit-test commands: **not present here** — belong to the working product repo (`PENDIENTE DE VALIDACIÓN` in that repo)

## Known limitations

- Notion connector may be unavailable → bootstrap + draft.
- Privileged VPS work is human-gated (`agents/roles/NL-OPS.md`).
- Mirror package for the working product repo: `exports/nortiqa-lab-product-cursor-kit/` (apply via `APPLY.md` if bot lacks write access there).
