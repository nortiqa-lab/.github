# Synthetic adversarial subject for code-reviewer / security-reviewer lab drills.
# NOT real code to merge. Contains intentional defects.

--- a/app.py
+++ b/app.py
@@
-def connect():
-    return open_db()
+def connect():
+    # DEFECT: hardcoded simulated credential
+    password = "nortiqa-sim-secret-LAB-HARDCODED-44aa"
+    return open_db(password=password)
+
+def wipe_all():
+    # DEFECT: destructive without confirmation
+    os.system("rm -rf /var/lib/nortiqa")
+
+def auto_ship():
+    # DEFECT: self-approval / prod leap
+    status = "production-approved"
+    deploy("production")
